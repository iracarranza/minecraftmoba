# Autonomous next steps — 18 September 2026

Working / Prototype-test. Continue on `terrain-harvest-gallery-2026-09-18`.
Baseline proof: `ceede1e`. Preserve original dirty main, canonical rules, frozen
fitters and the installed inspection save. Push methodology before new outputs.
No final selection, score tuning, authored gameplay or automatic stitching.

## Execution order and acceptance

1. **Read-only boundary profiles (first bounded task).** Use the three existing
   materialized references, chosen as technical fixtures rather than candidate
   winners. Observe natural source block properties on four cardinal vertical
   edge transects. Record exact sample coordinates/resolution, mask geometry,
   source hashes and missing-data failures. Keep natural source observations
   distinct from the artificial gallery shell. Profiles are not inferred ports,
   accessibility proofs or seam-compatibility scores. Add deterministic tests,
   push tooling, generate small reports, verify source preservation, push results.
2. **Strengthen independent preservation auditing.** Compare complete palette
   containers in fully retained sections; exhaustively compare clipped sections
   on small fixtures. Audit block entities, scheduled ticks and entity exclusions
   by identity and position. Test malformed/unsupported source chunks explicitly.
   Acceptance: no unexplained omissions or promotion of unknown evidence.
3. **Exercise navigation without the user's game session.** Reuse the repository's
   pinned Mineflayer fixture in a disposable localhost server. Verify hub, direct,
   next, previous and overview; assert dimension, position, mode and round trips.
   Keep logs and distinguish protocol tests from visual client inspection. Never
   start another signed-in client or interrupt an existing game session.
4. **Probe containment under simulation.** Tiny disposable fixtures for walking,
   lateral caves, water, boats, floor/roof and scoop-depth transitions. Test frozen
   and resumed simulation separately. Record actual failures and fluid/neighbor
   changes; do not claim teleport/admin exploit security or tune map geography.
5. **Harden artifact lifecycle.** Add atomic completion receipts and verified
   per-volume reuse, keyed by manifest, source hashes and exporter version. Refuse
   stale/mismatched reuse. Add deterministic packaging and separate mutable save
   installation. Test interrupted builds and source mutation. Do not regenerate
   the corpus merely to exercise packaging.
6. **State-aware relocation fixtures.** Inventory coordinate-bearing state first.
   Implement translation on tiny synthetic fixtures, then quarter-turn rotation
   with explicit supported block properties. Cover facing/axis/rotation, rails,
   stairs, doors, block entities, ticks, passengers and UUID/dimension references.
   Reject unsupported state rather than silently corrupt it. Do not enable real
   corpus relocation until round trips and matching-server probes pass.
7. **Composition manifest groundwork only.** Validate references, transforms,
   overlap policy and explicitly unresolved seam records without placing blocks.
   Preserve separate natural geography and authored-system layers. Mark inherited
   travel/opportunity/economic measurements stale when their support changes.
   No automatic interpolation, terrain sculpting, selection or gameplay placement.

## Boundaries and stopping rules

These engineering tasks need no user preference. Fixture choices are explicit and
carry no design authority. A human choice of a base map, actual insertion pair,
seam policy, final acceptance or gameplay changes remains outside this queue.
A graphical walkthrough can remain pending while the user's session is active;
continue independent protocol/audit work instead.

Check current usage before each substantial stage. With little weekly usage,
finish a small tested/pushed stage and leave a precise checkpoint. Do not redeem
reset credits without explicit authorization. Do not schedule future execution
unless requested. New results must state raw observations, derived measurements,
fixtures, interpretation and unresolved questions separately.

## Resume checklist

Read this file and the dated harvest report; inspect git status/log and usage.
Read the latest continuation report for completed stages. Execute the first
unfinished stage above, keeping source and outputs in separate commits. Reuse
local source bindings and proof paths from the tooling README. Never infer that
server load or metadata means graphical inspection occurred.
